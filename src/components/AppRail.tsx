/** * App shell navigation (design.md §10.3): * - Desktop/TV: fixed left rail, 72px collapsed → 224px on hover/focus *   (spring.smooth), glass-2 over --deep, "E" monogram → /app, active item *   gets cyan icon + 3px gradient bar + ink label, profile avatar at bottom. * - Mobile (<768px): bottom solid glass nav with 5 items + safe-area inset. */import { useState } from 'react';import { Link, NavLink } from 'react-router-dom';import { motion } from 'framer-motion';import {  BarChart3,  CalendarDays,  Compass,  Film,  Home,  LibraryBig,  MonitorPlay,  Puzzle,  Radio,  Search,  Settings,  User,} from 'lucide-react';import { LogoE } from '@/components/Logo';import { openCommandPalette } from '@/components/CommandPalette';import { spring } from '@/components/ui-elite';import { useT } from '@/i18n';import { useProfiles } from '@/lib/store';import { cn } from '@/lib/utils';const RAIL_ITEMS = [  { to: '/app', labelKey: 'app.rail.home', icon: Home, end: true },  { to: '/app/discover', labelKey: 'app.rail.discover', icon: Compass },  { to: '/app/movies', labelKey: 'app.rail.movies', icon: Film },  { to: '/app/series', labelKey: 'app.rail.series', icon: MonitorPlay },
